spring:
  redis:
    host: localhost
    port: 6379
    database: 0
    ###使用连接池其实没什么效果
    lettuce:
      pool:
        max-active: 32
        max-wait: 300
        max-idle: 16
        min-idle: 8
    timeout: 5000

